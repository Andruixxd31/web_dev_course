const a = require('fs'); 
//Reads the file system. 
//Can be used to read through a textfile and finding how much a word is repeated
const b = require('http'); 
//Useful for setting up servers.

console.log(a);
console.log(b);